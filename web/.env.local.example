# Copy this file to .env.local and fill in real values. .env.local is
# gitignored by default (see .gitignore) — never commit real secrets.

MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/uniclub
